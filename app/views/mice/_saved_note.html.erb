<% @user.saved_notes.each do |saved_note| %>
  <%= saved_note.created_at.localtime.strftime("Saved on %m/%d/%Y, %I:%M%P") %>
  <%= render partial: 'mice/note', locals: {note: saved_note.note}%>
<% end %>
